# Deprecation Status

## Recommended use

New readers should start with `persona-object-protocol`. Use this repo only when a standalone package wrapper is explicitly needed for compatibility or packaging continuity.

## Canonical parent repo

- `persona-object-protocol`

## Current posture

- deprecated standalone wrapper
- thin adapter surface only

## Future direction

This repo can remain as a lightweight packaging wrapper in the short term, but the canonical integration story should continue to consolidate into the POP repo.
